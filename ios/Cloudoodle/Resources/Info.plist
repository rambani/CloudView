<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>Cloudoodle</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>NSCameraUsageDescription</key>
    <string>Cloudoodle needs your camera to photograph the sky and find shapes in clouds.</string>
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>Cloudoodle uses your location to show cloud sightings near you and record where you spotted clouds.</string>
    <key>NSPhotoLibraryAddUsageDescription</key>
    <string>Cloudoodle can save your cloud sighting photos to your photo library.</string>
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>
    <key>UILaunchScreen</key>
    <dict>
        <!-- Warm darkroom backdrop matching the app's home views, so the
             cold-start flash transitions cleanly into camera-or-today
             without a color jump. Resolved via Assets.xcassets;
             fallback to black if the asset is missing so the launch
             is never broken. -->
        <key>UIColorName</key>
        <string>LaunchBackground</string>
        <!-- Centered cloud silhouette — same shape as the app icon's
             inner cloud, so tapping the icon and landing in the app
             feels continuous. Resolved via Assets.xcassets at 1x/2x/3x. -->
        <key>UIImageName</key>
        <string>LaunchLogo</string>
        <key>UIImageRespectsSafeAreaInsets</key>
        <true/>
    </dict>
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
        <string>camera</string>
    </array>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
    <!-- API keys injected at build time via Config.xcconfig — never hardcoded -->
    <key>GEMINI_API_KEY</key>
    <string>$(GEMINI_API_KEY)</string>
    <key>OPENAI_API_KEY</key>
    <string>$(OPENAI_API_KEY)</string>
    <key>SUPABASE_URL</key>
    <string>$(SUPABASE_URL)</string>
    <key>SUPABASE_ANON_KEY</key>
    <string>$(SUPABASE_ANON_KEY)</string>
    <key>SENTRY_DSN</key>
    <string>$(SENTRY_DSN)</string>
</dict>
</plist>
