# API keys and secrets — never commit
ios/Config.xcconfig
*.xcconfig
!*.xcconfig.example

# Xcode
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
DerivedData/
*.moved-aside
*.pbxuser
*.perspectivev3
*.xccheckout
*.xcscmblueprint
.DS_Store

# Swift Package Manager
.build/
ios/.build/

# Node (defensive — repo is Swift-only today, but tooling sometimes pulls in JS)
node_modules/

# CocoaPods (not used but just in case)
Pods/

# Environment files
.env
.env.local
*.pem
*.p8

# OS
.DS_Store
Thumbs.db
